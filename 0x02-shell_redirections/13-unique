#!/bin/bash
read -ra words; declare -A wordCounts; for word in "${words[@]}"; do ((wordCounts["$word"]++)); done; for word in "${!wordCounts[@]}"; do if ((wordCounts["$word"] == 1)); then echo "$word"; fi; done | sort
